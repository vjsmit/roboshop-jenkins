- name: Crate Jenkins Jobs
  hosts: localhost
  connection: local
  vars:
    jobs:
      - roboshop-terraform
      - cart
      - catalogue
      - user
      - frontend
      - shipping
      - payment
      - dispatch
  roles:
    - jenkins-jobs